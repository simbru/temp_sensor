# HDF5 data files (client-side)
templog.h5
templog_test*.h5
dev/templog_test*.h5

# LCD simulator frames and downloaded icons (generated during testing)
dev/lcd_frames/
dev/icons/

# SQLite database (server-side and test files)
sensor_data.db
*.db
*.db-shm
*.db-wal
*.meta.json

# Process ID files
tempsens_running.pid

# Runtime state files
.sensor_failures

# Claude Code local settings
.claude/settings.local.json

# Python cache
**/__pycache__/

# User settings
settings.ini

# Configuration files (site-specific, don't overwrite on pull)
config.ini
server/config_server.ini

# Internal documentation (security-sensitive)
HEADSCALE.md

# UV lock file (platform-specific, regenerate locally)
uv.lock

# Python virtual environment
.venv/

# Test config files - now in dev/ folder

# Service logs (Windows dashboard service)
logs/
*.log